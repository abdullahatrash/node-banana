# Layer Social Publish Validation

Social publishing will validate settings and media in both the client experience and the server/provider path. Client registry validation gives immediate feedback in Compose, while server and Provider Adapter validation protects API routes, tests the contract, and enforces platform rules closest to the external API. This deliberately avoids relying on client-only validation for platform-specific publish constraints.
