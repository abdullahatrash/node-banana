# Use Safe Defaults for Social Publishing Settings

Node Banana will initialize Publishing Settings with conservative defaults rather than copying every Postiz default. This means defaults should reduce the chance of accidental public publishing, while still requiring explicit creator choices for destination-specific fields such as Reddit subreddits and Pinterest boards. The trade-off is slightly more friction before publishing in exchange for safer handling of generated or experimental content.
