"use client";
import { useState, type FormEvent } from "react";
import { useRouter } from "next/navigation";
import { useTranslations } from "next-intl";
import { FolderPlus, LoaderCircle } from "lucide-react";
import { productRequest } from "@/components/product-surfaces/ProductApi";
export function MediaSetCreator() { const t = useTranslations("product.library"); const router = useRouter(); const [busy, setBusy] = useState(false); const [error, setError] = useState(""); async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); setBusy(true); const data = new FormData(event.currentTarget); try { await productRequest("/api/product-library/media-sets", { title: String(data.get("title")), assetIds: String(data.get("assetIds")).split(",").map((v) => v.trim()).filter(Boolean), category: String(data.get("category")), description: String(data.get("description")), idempotencyKey: crypto.randomUUID() }); event.currentTarget.reset(); router.refresh(); } catch (cause) { setError(cause instanceof Error ? cause.message : t("error")); } finally { setBusy(false); } } return <form onSubmit={submit} className="grid gap-3 rounded-2xl border bg-card p-4 sm:grid-cols-2 lg:grid-cols-4"><input name="title" required placeholder={t("setName")} className="min-h-11 rounded-xl border bg-background px-3" /><input name="category" placeholder={t("category")} className="min-h-11 rounded-xl border bg-background px-3" /><input name="assetIds" required placeholder={t("assetIds")} className="min-h-11 rounded-xl border bg-background px-3" /><input name="description" placeholder={t("descriptionField")} className="min-h-11 rounded-xl border bg-background px-3" /><button disabled={busy} className="inline-flex min-h-11 items-center justify-center gap-2 rounded-xl bg-amber-300 font-semibold text-stone-950 sm:col-span-2 lg:col-span-4">{busy ? <LoaderCircle className="size-4 animate-spin" /> : <FolderPlus className="size-4" />}{t("createSet")}</button>{error && <p role="alert" className="text-sm text-destructive sm:col-span-2 lg:col-span-4">{error}</p>}</form>; }
