import { SupportLayout } from "@/components/product-support/SupportLayout";
export default function RoadmapLayout({ children }: { children: React.ReactNode }) { return <SupportLayout path="/roadmap">{children}</SupportLayout>; }
