import { SupportLayout } from "@/components/product-support/SupportLayout";
export default function ReleasesLayout({ children }: { children: React.ReactNode }) { return <SupportLayout path="/releases">{children}</SupportLayout>; }
