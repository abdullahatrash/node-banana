import { SupportLayout } from "@/components/product-support/SupportLayout";
export default function CustomerSupportLayout({ children }: { children: React.ReactNode }) { return <SupportLayout path="/support">{children}</SupportLayout>; }
