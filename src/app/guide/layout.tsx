import { SupportLayout } from "@/components/product-support/SupportLayout";
export default function GuideLayout({ children }: { children: React.ReactNode }) { return <SupportLayout path="/guide">{children}</SupportLayout>; }
