import { SupportLayout } from "@/components/product-support/SupportLayout";
export default function FeedbackLayout({ children }: { children: React.ReactNode }) { return <SupportLayout path="/feedback">{children}</SupportLayout>; }
